import React from 'react';
import ReactDOM from 'react-dom/client';
import DepotSimulator from './DepotSimulator.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DepotSimulator />
  </React.StrictMode>
);
